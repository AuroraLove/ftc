<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.auroralove.ftctoken.mapper.UserMapper">
    <sql id="USER" >ftc_user</sql>
    <sql id="DEAL" >ftc_deal</sql>
    <sql id="PAYINFO" >ftc_user_payinfo</sql>
    <sql id="MESSAGE" >ftc_message</sql>

    <resultMap type="com.auroralove.ftctoken.model.AccountModel" id="DealAccountInfo">
        <result column="tradeableAcct" property="tradeableAcct"/>
        <result column="FTCLockedAcct" property="FTCLockedAcct"/>
    </resultMap>
    <resultMap type="com.auroralove.ftctoken.model.AccountModel" id="RewardAccount">
        <result column="FTCRewardAcct" property="FTCRewardAcct"/>
    </resultMap>
    <select id="findByUserphone" resultType="com.auroralove.ftctoken.model.UserModel">
      SELECT id,phone,name,password,admin_status as amdinStatus,parent_id as parentId,regist_flag as registFlag,regist_time as registTime,teamid
       FROM ftc_user
      where
      phone=#{phone}
    </select>

    <select id="findUserById" resultType="com.auroralove.ftctoken.model.UserModel">
        SELECT id,phone,password,teamid,admin_status,pay_pwd FROM ftc_user
      where
      id=#{id}
    </select>

    <insert id="newUser" parameterType="com.auroralove.ftctoken.model.UserModel">
        INSERT INTO
        <include refid="USER" />
        (
        id,
        parent_id,
        phone,
        password,
        teamid,
        regist_time
        )
        VALUES
        (
        #{id},
        #{parentId},
        #{phone},
        #{passWord},
        #{teamId},
        NOW()
        )
    </insert>

    <insert id="recharge" parameterType="com.auroralove.ftctoken.model.DealModel">
        INSERT INTO
        <include refid="DEAL" />
        (
        tid,
        uid,
        deal_amount,
        deal_date,
        type
        )
        VALUES
        (
        #{dealId},
        #{userId},
        #{amount},
        NOW(),
        #{type}
        )
    </insert>

    <update id="rechargeFlag">
        UPDATE <include refid="USER" />
        SET flag = #{flag}
        WHERE id = #{id}
    </update>
    
     <update id="changeLoginPwd">
        UPDATE <include refid="USER" />
        SET password = #{password}
        WHERE phone = #{phone}
    </update>
    
     <update id="changePayPwd">
        UPDATE <include refid="USER" />
        SET PAY_PWD = #{payPwd}
        WHERE id = #{id}
    </update>

    <select id="getPayInfo" resultType="com.auroralove.ftctoken.model.UserPayModel">
        SELECT pid,uid,name,card_id,bank_name,ali_pay,ali_url,wechat_pay,wechat_url,bank_address from ftc_user_payinfo
        WHERE uid = #{uid}
    </select>

    <update id="updatePayInfo" parameterType="com.auroralove.ftctoken.model.UserPayModel">
        update ftc_user_payinfo
        <set>
        <if test=" name!=null">
            name=#{name},
        </if>
            <if test="phone !=null">
                phone=#{phone},
            </if>
            <if test="card_id !=null">
               card_id=#{card_id},
            </if>
            <if test="bank_name !=null">
                bank_name=#{bank_name},
            </if>
            <if test="ali_pay !=null">
                ali_pay=#{ali_pay},
            </if>
            <if test=" ali_url!=null">
                ali_url=#{ali_url},
            </if>
            <if test="wechat_pay !=null">
                wechat_pay=#{wechat_pay},
            </if>
            <if test="wechat_url !=null">
                wechat_url=#{wechat_url},
            </if>
        </set>
        where uid = #{uid}
    </update>

    <select id="getDealAccountInfo" resultMap="DealAccountInfo" >
select (buy_release_amount- sell_amount) as tradeableAcct,FTCLockedAcct from
(select uid,sum(deal_amount) as FTCLockedAcct from ftc_deal  where type=2 GROUP BY uid)as t1 LEFT  JOIN
 (select uid,sum(deal_amount) as buy_release_amount from ftc_deal  where type in(0,7) and status=6 GROUP BY uid)  as t2 ON t1.uid = t2.uid LEFT  JOIN
   (select uid,sum(deal_amount) as sell_amount from ftc_deal  where type=1 and status=6 GROUP BY uid)   as t3 ON t3.uid = t2.uid
where t1.uid= #{uid}
    </select>

    <select id="getRewardAccount"  resultMap="RewardAccount" >
 select sum(amount) as FTCRewardAcct  from ftc_reward
where uid= #{id} GROUP BY uid
    </select>

    <select id="getPublicMsg" resultType="com.auroralove.ftctoken.model.PublicInfoModel">
     SELECT pid,information,date from ftc_publicinfo
    </select>

    <insert id="newPublicMsg" parameterType="java.lang.String">
       insert into ftc_publicinfo(information,date)
       VALUES (#{information},NOW())
    </insert>

    <delete id="deletePublicMsg" parameterType="java.lang.Long">
        delete from ftc_publicinfo where pid = #{pid}
    </delete>

    <insert id="uploadMsg" >
        INSERT INTO
        <include refid="MESSAGE" />
        (id,uid,leaving_date,  msg, pic_url)
        VALUES
        (
        #{id},
        #{uid},
        NOW(),
        #{message},
        #{url}
        )
    </insert>

    <update id="replayMsg">
        UPDATE
        <include refid="MESSAGE"/>
        set
        replay_date = NOW(),
        r_msg = #{message}
        WHERE id=#{mesId}
    </update>

    <select id="messageInfo" resultType="com.auroralove.ftctoken.model.MessageModel">
        SELECT msg,r_msg,leaving_date,replay_date FROM ftc_message
      where
      uid=#{uid}
    </select>

    <select id="getUserChilds" resultType="com.auroralove.ftctoken.model.UserModel">
        SELECT id,phone,name,regist_flag
         FROM ftc_user
      where
      parent_id=#{uid} and regist_flag=1
    </select>

    <insert id="savePayInfo">
        INSERT INTO ftc_user_payinfo
        (pid,uid,name,phone,card_id,bank_name,ali_pay,ali_url,wechat_pay,
        wechat_url)
        VALUES
        (
        #{pid},
        #{uid},
        #{name},
        #{phone},
        #{card_id},
        #{bank_name},
        #{ali_pay},
        #{ali_url},
        #{wechat_pay},
        #{wechat_url}
        )
    </insert>

    <update id="updatePayPwd">
        update ftc_user
        <set>
        pay_pwd = #{payPwd}
        </set>
        where id = #{id}
    </update>
</mapper>